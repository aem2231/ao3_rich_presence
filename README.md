
## Discord Rich Presence for AO3

Displays whatever you're currently reading on ao3 in your Discord status — including fic title, chapter, and author.

## Usage

1. Run `rpc_server.py` manually as needed, or add it as a systemd service so it starts on boot.
2. Build and install the browser extension:
   ```sh
   `web-ext build --source-dir extension/`

